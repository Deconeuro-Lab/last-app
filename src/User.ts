namespace User {
  type Examiner = {
    firstName: string;
    lastName: string;
  };

  type Patient = {
    firstName: string;
    lastName: string;
  };
}

// TODO: export the User namespace
export type FixMeLater = any;
